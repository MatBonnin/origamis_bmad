services:
  postgres:
    image: postgres:16-alpine
    container_name: origami-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Eqlulfyk38
      POSTGRES_DB: origami
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
